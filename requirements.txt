# Fallom SDK dependencies
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
requests>=2.28.0

# Optional: Auto-instrumentation for LLM providers
# pip install fallom[openai] or fallom[anthropic] or fallom[all]

